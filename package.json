{
    "name": "fullstack-vite-node-app",
    "version": "1.0.0",
    "engines": {
      "node": "22.11.0",
      "npm": "11.0.0"
    },
   "scripts": {
  "heroku-postbuild": "npm install --prefix backend && npm run build --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
  "clean-public": "rm -rf backend/dist/public/*"
},
    "dependencies": {},
    "devDependencies": {}
  }
  