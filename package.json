{
    "name": "fullstack-vite-node-app",
    "version": "1.0.0",
    "engines": {
      "node": "22.11.0",
      "npm": "11.0.0"
    },
    "scripts": {
      "start": "npm start --prefix backend",
      "preinstall": "npm install --only=dev --prefix backend",
      "heroku-postbuild": "npm install --prefix backend && npm install --only=dev --prefix backend && npm install --prefix frontend && npm install --only=dev --prefix frontend && npm run build --prefix backend && npm run build --prefix frontend",
      "install-all": "npm install --prefix backend && npm install --prefix frontend", 
      "build-all": "tsc --prefix backend && npm run build --prefix frontend" 
    },
    "dependencies": {},
    "devDependencies": {}
  }

