{
    "name": "fullstack-vite-node-app",
    "version": "1.0.0",
    "engines": {
      "node": "22.11.0",
      "npm": "11.0.0"
    },
    "scripts": {
      "start": "npm run build-backend && node backend/dist/app.js",
      "preinstall": "npm install --only=dev --prefix backend",
      "build-backend": "npm install --prefix backend && npm install tsc --prefix backend && npx tsc --project backend/tsconfig.json",
      "build-frontend": "npm install --prefix frontend && npm run build --prefix frontend",
      "heroku-postbuild": "npm run build-backend && npm run build-frontend",
      "install-all": "npm install --prefix backend && npm install --prefix frontend",
      "build-all": "tsc-backend && npm run build-frontend"
    },
    "dependencies": {},
    "devDependencies": {}
  }
  