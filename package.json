{
    "name": "fullstack-vite-node-app",
    "version": "1.0.0",
    "engines": {
      "node": "22.11.0",
      "npm": "11.0.0"
    },
    "scripts": {
      "start": "node backend/dist/app.js", 
      "preinstall": "npm install --only=dev --prefix backend",
      "heroku-postbuild": "npm install --prefix backend && npm install --prefix frontend && npx tsc --project backend/tsconfig.json && npm run build --prefix frontend",
      "install-all": "npm install --prefix backend && npm install --prefix frontend",
      "build-all": "npx tsc --project backend/tsconfig.json && npm run build --prefix frontend"
    },
    "dependencies": {},
    "devDependencies": {}
  }
  