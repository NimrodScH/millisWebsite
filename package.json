{
    "name": "fullstack-vite-node-app",
    "version": "1.0.0",
    "engines": {
        "node": "22.11.0",
        "npm": "11.0.0"
    },
    "scripts": {
  "build-backend": "npm install --prefix backend && npm run build --prefix backend",
  "build-and-start": "npm run build-backend && npm start --prefix backend"
            },

    
    "devDependencies": {
        "husky": "^9.1.7",
        "require-so-slow": "^2.0.5"
    }
}
