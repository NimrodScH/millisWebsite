{
    "name": "fullstack-vite-node-app",
    "version": "1.0.0",
    "engines": {
      "node": "22.11.0",
      "npm": "11.0.0"
    },
    "scripts": {
      "start": "npm run start --prefix backend",
      "preinstall": "npm install --only=dev --prefix backend",
      "heroku-postbuild": "NODE_ENV=development npm install --prefix backend && npm run build --prefix backend && NODE_ENV=development npm install --prefix frontend && npm run build --prefix frontend"
    },
    "dependencies": {},
    "devDependencies": {}
  }
  