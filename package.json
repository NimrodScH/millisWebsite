{
    "name": "fullstack-vite-node-app",
    "version": "1.0.0",
    "engines": {
      "node": "22.11.0",
      "npm": "11.0.0"
    },
    "scripts": {
      "start": "npm run start --prefix backend",
      "heroku-postbuild": "npm install --prefix backend && npm install -g typescript && npm run build --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
    },
    "dependencies": {},
    "devDependencies": {}
  }
  